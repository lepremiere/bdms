

def clean_database(
        root_dir: str,
    ) -> None:
    """
    Clean the database by removing daily elements if monthly elements exist,
    that contain the same data.
    
    Parameters
    ----------
    root_dir : str
        The root directory of the database.
    """
    raise NotImplementedError